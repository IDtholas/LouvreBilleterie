{% extends 'base.html.twig' %}

{% block body %}
    <aside class="banner">

        <div class="container">

            <div class="row">
                <div class="col-lg-12 my-auto">
                    <h2>Complétez votre commande</h2>
                    <p> Merci de remplir les informations demandées</p>
                </div>
            </div>

        </div>
        <!-- /.container -->
    </aside>

    </br>

    <div class="container">

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                <h1>{{ message }}</h1>
            </div>
        {% endfor %}
        <div class="row">
            <div> <h1 class="text-center">Merci d'avoir passé commande</h1>
                <p> Un email de confirmation vient de vous être envoyé.</p>
                <a href="{{ path('louvre_accueil') }}" class="btn btn-secondary btn-lg">Retour à l'accueil</a>
            </div>
        </div></br>
    </div>
{% endblock %}